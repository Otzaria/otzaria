# דוגמאות שימוש - פתיחת קישורים דרך תיבת החיפוש

## איך להשתמש בפיצר החדש

### שלב 1: פתח את מסך הספרייה
עבור למסך הספרייה באפליקציה (הטאב הראשון).

### שלב 2: הזן קישור בתיבת החיפוש
במקום לחפש ספר, הזן אחד מהקישורים הבאים:

#### דוגמאות לקישורים מלאים:
```
otzaria://book/ברכות?page=2
otzaria://pdf/רמבם?page=15
book://ברכות#דף ב
```

#### דוגמאות לקישורים מקוצרים:
```
ברכות?page=2
רמבם?index=10
#דף ג
```

### שלב 3: שים לב לאינדיקציה החזותית
כאשר המערכת מזהה שהטקסט הוא קישור:
- האייקון משתנה לאייקון קישור 🔗
- הטקסט מקבל צבע כחול
- מופיע אייקון חץ עם הודעה "לחץ Enter לפתיחת הקישור"
- המסגרת של תיבת החיפוש מקבלת צבע כחול

### שלב 4: לחץ Enter
לחיצה על Enter תפתח את הספר המתאים בטאב חדש.

## פורמטים נתמכים

### 1. קישורי otzaria:// מלאים
- `otzaria://book/שם_ספר?page=מספר` - לספרי טקסט
- `otzaria://pdf/שם_ספר?page=מספר` - לספרי PDF
- `otzaria://inline-link?path=נתיב&index=אינדקס&ref=הפניה` - קישורים מבוססי תווים

### 2. קישורי book://
- `book://שם_ספר` - פתיחת ספר מההתחלה
- `book://שם_ספר#כותרת` - פתיחת ספר במקום ספציפי

### 3. קישורים פנימיים
- `#כותרת` - מעבר לכותרת באותו ספר (אם יש ספר פתוח)

### 4. קישורים מקוצרים
המערכת תנסה לזהות אוטומטית:
- `שם_ספר?page=מספר` - יזוהה כקישור לספר
- `path=נתיב&index=אינדקס` - יזוהה כקישור inline

## פרמטרים נתמכים

| פרמטר | תיאור | דוגמה |
|--------|--------|--------|
| `page` | מספר עמוד (בעיקר לPDF) | `?page=15` |
| `index` | אינדקס המקטע בספר | `?index=10` |
| `text` | טקסט להדגשה | `?text=הלכה` |
| `path` | נתיב מדויק לקובץ | `?path=/books/ברכות.txt` |
| `ref` | הפניה או תיאור | `?ref=דף ב` |

## טיפים לשימוש

1. **העתק קישורים מהאפליקציה** - השתמש בפונקציית השיתוף הקיימת כדי ליצור קישורים
2. **שמור קישורים מועדפים** - שמור קישורים לספרים שאתה משתמש בהם הרבה
3. **שתף עם אחרים** - העבר קישורים לחברים כדי שיוכלו לפתוח את אותו המקום בספר
4. **שימוש מהירה** - הדבק קישור ולחץ Enter במקום לנווט דרך התפריטים

## פתרון בעיות

### הקישור לא נפתח
- ודא שהספר קיים בספרייה שלך
- בדוק שהפורמט של הקישור נכון
- נסה להסיר רווחים מיותרים

### הטקסט לא מזוהה כקישור
- ודא שהקישור מתחיל באחד הפרוטוקולים הנתמכים
- בדוק שיש פרמטרים כמו `?page=` או `?index=`

### הספר נפתח במקום הלא נכון
- בדוק את הפרמטרים `page` או `index`
- ודא שמספרי העמודים/אינדקסים נכונים