# issue #1410 — עדכון תוסף שאין בו מה לאשר פותח דיאלוג מיותר

**ענף:** `fix/plugin-plain-update-1410` על `upstream/dev` f47ff65b7 · **מיילסטון:** 0.9.98 · **פלטפורמה:** macOS 27.0 (Apple Silicon) · **Flutter:** 3.47.2 (זהה ל-CI) · **תאריך:** 20.9.2026

## הבקשה
"לאחר עדכון תוסף, מופיע חלון אישור — גם אם התוסף לא דורש הרשאה נוספת. יש לבטל חלון
זה, נראה שכדאי להקפיץ פופאפ בסגנון 'התוסף עודכן'. אין טעם לדרוש אישור משתמש
(במיוחד שהוא עצמו ביקש את העדכון)."

## השורש
`PluginInstallScreen` **כבר יודע** לזהות את המקרה: יש בו getter פרטי
`_isPlainUpdate` — "עדכון שאינו דורש שום החלטה" — שבודק שאין הרשאות חדשות, אין
הרשאות כבויות ואין שאלת הקדמה פתוחה. כשהוא מתקיים המסך מציג כרטיס יחיד,
"העדכון אינו מבקש הרשאות חדשות".

אלא שהכלל היה *פרטי למסך*, והמארח — `_showPluginInstallDialog` ב-`main_window_screen`,
שהוא זה שמחליט אם בכלל לפתוח את המסך — לא יכול היה לראות אותו. לכן הוא פתח דיאלוג
על כל `PluginSystemInstallRequiresPermissions`, כולל כשאין בו מה להחליט.

הפופאפ שהאישו ביקש כתחליף כבר קיים: `plugin_system_bloc.dart:512` שולח
`UiSnack.showSuccess(PluginMessages.pluginUpdatedSuccess)` — "התוסף עודכן בהצלחה" —
בסיום כל עדכון.

## התיקון
החישובים הוצאו מהמסך ל-`resolvePluginInstallDecision`, פונקציה טהורה
(`lib/plugins/models/plugin_install_decision.dart`). המסך צורך אותה במקום לחשב
בעצמו — **46 הבדיקות הקיימות של המסך עוברות ללא שינוי**, כלומר ההעברה אינה משנה
התנהגות. המארח שואל את אותה פונקציה דרך `resolvePluginInstallPrompt`, ועדכון שאין
בו החלטה נשלח ישירות ל-`ConfirmPluginInstall` עם ההרשאות השמורות, בלי דיאלוג.

הפונקציה הטהורה יושבת ב-`main_window_screen.dart` באותו דפוס של
`resolveLibraryPageBuildDecision` שכבר קיים בקובץ ונבדק כך — זו הדרך שהמאגר עצמו
בוחר לבדוק החלטות של המסך הזה בלי לרנדר אותו.

אין קוד חדש להודעה ואין הגדרה חדשה.

**מה ממשיך לעצור לאישור:** הרשאה חדשה, הרשאה שהמשתמש כיבה, הרשאת רשת שאינה זמינה
במצב מנותק, שאלת ההקדמה לפני כלים מובנים שטרם נענתה, וכל התקנה ראשונה. באישו יש
הסתייגות של palmoni5 ("בפועל הוא צריך לאשר, כמו כל פעולה") — התיקון חל רק על עדכון
שאינו מבקש דבר, ולכן כל בקשת גישה חדשה נשארת כשהייתה.

**הכרטיס במסך נשאר** עבור עדכון תוסף פיתוח: שם אין החלטות הרשאה שמורות, ולכן המסלול
עדיין יכול להגיע אליו.

## בדיקות — `test/plugins/models/plugin_install_decision_test.dart`

| בדיקה (issue #1410) | לפני | אחרי |
|---|---|---|
| כל ההרשאות כבר הוענקו — אין מה לשאול | ❌ קומפילציה נכשלת: הכלל אינו קיים | ✅ |
| תוסף בלי הרשאות כלל — אין מה לשאול | ❌ אותו כשל | ✅ |
| הרשאה חדשה בגרסה — שואלים | ❌ אותו כשל | ✅ |
| הרשאה שהמשתמש כיבה — שואלים | ❌ אותו כשל | ✅ |
| הרשאת רשת שאינה זמינה במצב מנותק — שואלים | ❌ אותו כשל | ✅ |
| שאלת ההקדמה שטרם נענתה — שואלים | ❌ אותו כשל | ✅ |
| התקנה ראשונה תמיד שואלת | ❌ אותו כשל | ✅ |
| המארח: עדכון בלי הרשאות חדשות אינו נפתח לדיאלוג | ❌ אותו כשל | ✅ |
| המארח: עדכון עם הרשאה חדשה כן נפתח לדיאלוג | ❌ אותו כשל | ✅ |

הכשל לפני התיקון הוא בקומפילציה, וזה הממצא עצמו: לא היה בתוכנה שום כלל שמחליט אם
עדכון משאיר החלטה למשתמש.

`flutter test test/plugins/ test/navigation/` — 2,352 עברו, 19 דולגו, 0 נכשלו.
`flutter analyze` על שלושת הקבצים — No issues found. `dart format` הורץ על כולם.

## סוויטה מלאה מול הבסיס
הסוויטה של הענף רצה לבדה, מול בסיס `dev` של אותו קומיט `f47ff65b7` שרץ היום:

| | עברו | דולגו | נכשלו |
|---|---|---|---|
| `dev` | 13,010 | 272 | 32 |
| הענף | 13,018 | 272 | 33 |

31 מ-33 הכשלים מופיעים גם בבסיס. שני הנוספים — שניהם
ב-`text_book_bloc_test.dart` (`מתעלם מעדכון visibleIndices שגוי`,
`ApplyMarkHighlight עם highlightText ריק`) — הורצו לבדם על הענף ו**עברו**
(53 בדיקות, All tests passed). הקובץ הזה מתנדנד תחת עומס בכל אחת מארבע הסוויטות
שהרצתי היום, בכל פעם על בדיקות אחרות ולשני הכיוונים. התיקון נוגע בשכבת התוספים
ובמארח ואינו נוגע ב-`TextBookBloc` בשום מסלול.

## אימות חזותי — לפני/אחרי
בניתי תוסף בדיקה בשלוש גרסאות (`.otzplugin`) והתקנתי אותו דרך "התקן תוסף חדש" —
מסלול ההתקנה מקובץ, זה שהאישו מדבר עליו, ולא מסלול הפיתוח. כל שלוש הגרסאות מבקשות
בדיוק את אותה הרשאה אחת (`search.fulltext.read`).

| | |
|---|---|
| לפני (בנייה של `dev` נקי) | ![לפני](https://raw.githubusercontent.com/YMP112/otzaria/pr-screenshots/1410/1410-before.png) |
| אחרי (בנייה של הענף) | ![אחרי](https://raw.githubusercontent.com/YMP112/otzaria/pr-screenshots/1410/1410-after.png) |

**לפני:** הותקנה 1.0.0 (דיאלוג התקנה ראשונה — תקין), ואז 1.0.1. עלה דיאלוג
"עדכון תוסף: בדיקת עדכון", `1.0.0 ← 1.0.1`, שכל תוכנו הכרטיס "העדכון אינו מבקש
הרשאות חדשות", עם כפתורי עדכן/ביטול. אין בו מה להחליט.

**אחרי:** הותקנה 1.0.2 על אותו תוסף. לא עלה שום דיאלוג, והתוכנה הציגה את ההודעה
"התוסף עודכן בהצלחה".

שני הצילומים הם חלון שלם של אותה תוכנה באותו פרופיל, כדי שייראה גם מה שקרה וגם
מה שלא קרה.
