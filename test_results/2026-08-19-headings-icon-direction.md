# תוצאות טסטים — אייקון שורות הכותרות הפוך מאייקון לשונית "כותרות"

תאריך: 2026-08-19, ענף `fix/headings-icon-direction` (בסיס dev `6c817f38`)
Flutter 3.44.0 / Dart 3.12.0, Windows 11

## מיפוי (קומיט `fb9975ee`)

לשונית "כותרות" משתמשת ב-`OtzariaIcons.text_bullet_list_24_regular`
(גליף מותאם-RTL מחבילת otzaria_icons), אבל שורות הכותרות עצמן ציירו את
`FluentIcons.text_bullet_list_24_regular` — גליף LTR: אותו סימן הפוך
כיוון, זה מול זה באותה חלונית. טסט ווידג'ט חדש
ב-`toc_navigator_screen_test.dart` נכשל על הקוד הקיים (9 הקיימים עברו).

## אחרי התיקון (קומיט `56bb3b2a`)

שבעה אתרים הוחלפו לגליף האחיד: ניווט (2), כותרות חלופיות (2), ניווט
PDF‏ (2), לשוניות המפרשים בטקסט וב-PDF‏ (2×1). כפתורי "רשימת תבליטים"
בעורכי הטקסט (מחברת אישית, עורך markdown) נשארו בכוונה על גליף Fluent —
הם פעולת עיצוב טקסט, לא שורת כותרת.

- `flutter test test/text_book/view/toc_navigator_screen_test.dart` —
  **10 עברו, 0 נכשלו** (כולל טסט המיפוי).
- `dart analyze` על חמשת קבצי הקוד — נקי.

## סוויטה מלאה

`flutter test`: **8,351 עברו, 5 דולגו, 7 נכשלו** — כולם קיימים-מראש
וסביבתיים, אף אחד לא באזור השינוי: אותם שישה מהענפים הקודמים (bash
ב-release_packaging, סף כיווץ SQLite ×3, נעילת קובץ DOCX, מפריד נתיבים
ב-custom_folder_model) + `settings_l10n_test` תלוי-המכונה (הגנרטור המקומי
מפיק settings_catalogs.g.dart שונה מהגרסה שבריפו — אובחן בענף ההדגשה).

## בנייה

`flutter build windows --debug` — הצליחה; לאימות ידני: פתחו ספר,
בחלונית הצד השוו את אייקון לשונית "כותרות" לאייקוני שורות הכותרות
ב"ניווט" וב"כותרות" — עכשיו אותו גליף באותו כיוון; גם בניווט של PDF
ובלשוניות המפרשים.
