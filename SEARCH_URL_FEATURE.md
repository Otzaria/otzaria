# פתיחת קישורים דרך תיבת החיפוש

## תיאור הפיצר
נוספה אפשרות לפתוח קישורים ישירות דרך תיבת החיפוש במסך הספרייה. כאשר המשתמש מזין קישור בתיבת החיפוש ולוחץ Enter, המערכת תזהה את הקישור ותפתח את הספר המתאים.

## ארגון הקוד

### מודול קישורים חדש
כל הקוד הקשור לקישורים מאורגן במודול ייעודי: `lib/links/`

#### מבנה המודול:
```
lib/links/
├── core/                    # לוגיקה מרכזית
│   ├── link_handler.dart    # טיפול בקישורים
│   ├── link_parser.dart     # פענוח קישורים
│   └── link_generator.dart  # יצירת קישורים
├── models/                  # מודלי נתונים
│   └── link_models.dart     # BookLink, LinkType, etc.
├── ui/                      # אינטגרציה עם UI
│   ├── search_box_link_handler.dart
│   ├── sharing_links.dart
│   └── context_menu_links.dart
├── utils/                   # כלי עזר
│   └── url_encoding.dart    # קידוד URLs
├── links.dart              # ייצוא כל המחלקות
└── README.md               # תיעוד המודול
```

### קבצים שהועברו/שונו:
- `lib/utils/search_url_handler.dart` → `lib/links/ui/search_box_link_handler.dart`
- `lib/utils/html_link_handler.dart` → עכשיו wrapper ל-LinkHandler החדש
- `lib/library/view/library_browser.dart` → עודכן להשתמש במודול החדש

## איך זה עובד

### זיהוי קישורים
המערכת מזהה אוטומטית קישורים בפורמטים הבאים:
- `otzaria://book/שם_ספר?index=מספר&text=טקסט` - קישורים לספרי טקסט
- `otzaria://pdf/שם_ספר?page=מספר` - קישורים לספרי PDF
- `otzaria://inline-link?path=נתיב&index=אינדקס&ref=הפניה` - קישורים מבוססי תווים
- `book://שם_ספר#כותרת` - קישורים פשוטים לספרים
- `#כותרת` (קישור פנימי) - מעבר לכותרת באותו ספר
- קישורים מקוצרים כמו `שם_ספר?page=מספר` - זיהוי אוטומטי

### אינדיקציה חזותית
כאשר המערכת מזהה קישור:
- האייקון בתיבת החיפוש משתנה לאייקון קישור
- הטקסט מקבל צבע כחול
- מופיע אייקון חץ עם הודעה "לחץ Enter לפתיחת הקישור"
- המסגרת של תיבת החיפוש מקבלת צבע כחול

### פתיחת הקישור
- לחיצה על Enter פותחת את הקישור
- הספר נפתח בטאב חדש במסך הקריאה
- תיבת החיפוש מתנקה אוטומטית
- המערכת עוברת למסך הקריאה

## שימוש במודול החדש

### פתיחת קישור:
```dart
import 'package:otzaria/links/links.dart';

// פתיחת קישור באפליקציה
await LinkHandler.openLinkInApp(context, 'otzaria://book/ברכות?index=5');
```

### יצירת קישור:
```dart
// יצירת קישור לספר
final url = LinkGenerator.createSharingUrl(book, position: 10);

// שיתוף קישור
await SharingLinks.shareBookLink(context, book, position: 10);
```

### בדיקת תקינות:
```dart
if (LinkParser.isValidUrl(text)) {
  // הטקסט הוא קישור תקין
}
```

## יתרונות הארגון החדש

1. **הפרדה ברורה** - כל הקוד הקשור לקישורים במקום אחד
2. **מודולריות** - כל מחלקה אחראית על תחום ספציפי
3. **תאימות לאחור** - הקוד הקיים ממשיך לעבוד
4. **הרחבה קלה** - קל להוסיף סוגי קישורים חדשים
5. **בדיקות** - מבנה שמאפשר בדיקות יחידה
6. **תחזוקה** - קל למצוא ולתקן בעיות

## דוגמאות שימוש

### קישורים מלאים:
```
otzaria://book/ברכות?page=2
otzaria://pdf/רמבם?page=15
book://ברכות#דף ב
```

### קישורים מקוצרים:
```
ברכות?page=2
רמבם?index=10
#דף ג
```

## פרמטרים נתמכים

| פרמטר | תיאור | דוגמה |
|--------|--------|--------|
| `page` | מספר עמוד (בעיקר לPDF) | `?page=15` |
| `index` | אינדקס המקטע בספר | `?index=10` |
| `text` | טקסט להדגשה | `?text=הלכה` |
| `path` | נתיב מדויק לקובץ | `?path=/books/ברכות.txt` |
| `ref` | הפניה או תיאור | `?ref=דף ב` |

## הערות טכניות
- המודול משתמש בעקרונות SOLID
- כל מחלקה יחידה באחריות
- קל להרחיב ולתחזק
- תמיכה מלאה בקידוד URL ובטיפול בשגיאות
- אינטגרציה חלקה עם המערכת הקיימת