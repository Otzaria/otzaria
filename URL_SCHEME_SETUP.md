# הגדרת קישורי otzaria:// במערכות הפעלה שונות

מדריך זה מסביר כיצד להגדיר את האפליקציה אוצריא כמטפלת ברירת מחדל עבור קישורים שמתחילים ב-`otzaria://`.

## Android

ההגדרה מתבצעת אוטומטית בעת התקנת האפליקציה. ה-`AndroidManifest.xml` כבר מכיל את ההגדרות הנדרשות.

**בדיקה:**
- פתח דפדפן באנדרואיד
- הקלד בשורת הכתובת: `otzaria://book/בראשית`
- האפליקציה אמורה להיפתח אוטומטית

## iOS

ההגדרה מתבצעת אוטומטית בעת התקנת האפליקציה. ה-`Info.plist` כבר מכיל את ההגדרות הנדרשות.

**בדיקה:**
- פתח Safari ב-iOS
- הקלד בשורת הכתובת: `otzaria://book/בראשית`
- האפליקציה אמורה להיפתח אוטומטית

## Windows

### אפליקציית MSIX (מ-Microsoft Store)
אם התקנת את האפליקציה דרך Microsoft Store או כ-MSIX package, הרץ את הסקריפט הבא כמנהל:

```powershell
# פתח PowerShell כמנהל
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\windows\register_url_scheme.ps1
```

### אפליקציה רגילה
אם יש לך את קובץ ה-exe של האפליקציה, הרץ:

```powershell
.\windows\register_url_scheme.ps1 -AppPath "C:\path\to\otzaria.exe"
```

### רישום ידני
לחלופין, ניתן לייבא את קובץ ה-registry:
1. ערוך את הקובץ `windows\register_url_scheme.reg`
2. עדכן את הנתיב לקובץ ה-exe שלך
3. לחץ פעמיים על הקובץ לייבוא

**בדיקה:**
- פתח דפדפן ב-Windows
- הקלד בשורת הכתובת: `otzaria://book/בראשית`
- האפליקציה אמורה להיפתח אוטומטית

## Linux

הרץ את סקריפט ההתקנה:

```bash
# ודא שהאפליקציה נבנתה
flutter build linux

# הרץ את סקריפט ההתקנה
./linux/install_url_scheme.sh
```

**בדיקה:**
- פתח דפדפן ב-Linux
- הקלד בשורת הכתובת: `otzaria://book/בראשית`
- האפליקציה אמורה להיפתח אוטומטית

## macOS

ההגדרה מתבצעת אוטומטית בעת התקנת האפליקציה. ה-`Info.plist` כבר מכיל את ההגדרות הנדרשות.

**בדיקה:**
- פתח Safari ב-macOS
- הקלד בשורת הכתובת: `otzaria://book/בראשית`
- האפליקציה אמורה להיפתח אוטומטית

## פתרון בעיות

### Windows
- ודא שאתה מריץ את PowerShell כמנהל
- בדוק שהנתיב לקובץ ה-exe נכון
- אם זה לא עובד, נסה לאתחל את המחשב

### Linux
- ודא שיש לך הרשאות כתיבה ל-`~/.local/share/applications`
- בדוק שהחבילות `xdg-utils` מותקנות
- אם זה לא עובד, נסה להתנתק ולהתחבר מחדש

### בדיקה כללית
אם הקישורים לא עובדים, בדוק:
1. שהאפליקציה מותקנת ופועלת
2. שההגדרות נרשמו בהצלחה
3. שאין אפליקציה אחרת שכבר רשומה לטפל ב-`otzaria://`

## דוגמאות לקישורים

- `otzaria://book/בראשית` - פתיחת ספר בראשית
- `otzaria://book/בראשית?index=100` - פתיחת ספר בראשית בעמוד 100
- `otzaria://pdf/משנה ברורה` - פתיחת PDF של משנה ברורה
- `otzaria://pdf/משנה ברורה?page=50` - פתיחת PDF בעמוד 50